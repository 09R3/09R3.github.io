# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=waterops
DB_USER=your_username
DB_PASSWORD=your_password

# Server settings (Docker overrides PORT/HOST via docker-compose)
PORT=3000
HOST=0.0.0.0
